env: standard
runtime: nodejs22
service: default
handlers:
  - url: /.*
    secure: always
    script: auto

instance_class: F1

automatic_scaling:
  min_instances: 1
  max_instances: 5
  min_idle_instances: 1
  max_idle_instances: 5
  target_cpu_utilization: 0.8
  max_concurrent_requests: 80